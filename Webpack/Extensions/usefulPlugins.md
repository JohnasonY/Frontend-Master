html-webpack-plugin

copy-webpack-plugin

webpack-dev-server (not a loader nor a plugin)

file-loader: generate require files to dist, the loader translates file contents into an url

url-loader: the loader translates file contents into base64 format string
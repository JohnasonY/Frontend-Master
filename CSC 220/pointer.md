py: The variable address where py points to
\*py: The variable value that py points to
&py: py's address

%d: print out the value in decimal
%x: print out the value in hexiademical
%p: print out address such as for px, py
%zu: print out the bytes value for sizeof()

uname -m: check machine architecture (CPU + OS mode). E.X:
